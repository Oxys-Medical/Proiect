#include "ServerLayer.h"

void ServerLayer::SendData()
{
    // Trimitem datele prin HTTP?
    // sau le salvam intr-o baza de date? 
    // daca le salvam intr-o baza de date, acest lucru nu ar trebui facut in DataLayer/StorageLayer?
}